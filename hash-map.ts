


class HashMap {
    private hashMap: any[] = new Array(999999999);



    hashMapCala(key: any) {
        // key -> home - ohme -> 5640

        // hashMap[5640] -> 5, 7, 8,1
        // 
    }
    


    add(key, value) {
        //const key = "home";
        const position = 0; // the number that calculated from toNymber(key)
    
    
    
        this.hashMap[position].push({key, value]) // O(ln-n)
    }
}

[key] -> [key, value]


arry[key] = []


"home" -> 

